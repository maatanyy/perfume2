# Flask 및 웹 프레임워크
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.2.1
Flask-Limiter==3.5.0
WTForms==3.1.1

# 데이터베이스
SQLAlchemy==2.0.23

# 인증 및 보안
bcrypt==4.1.2
python-dotenv==1.0.0
psutil==5.9.6

# 작업 큐 (선택사항)
Celery==5.3.4
redis==5.0.1

# 크롤링
requests==2.31.0
beautifulsoup4==4.12.2
selenium==4.15.2
undetected-chromedriver==3.5.4  # 봇 감지 우회 Chrome 드라이버
setuptools>=68  # py3.12: undetected-chromedriver가 쓰는 distutils 제공
lxml>=6.1.1  # scrapling이 lxml>=6.1.1 요구 — 4.9.3 고정 시 pip 해석 불가(무한 백트래킹)

# 구글 시트 연동
google-api-python-client==2.108.0
google-auth==2.25.2
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.1.1

# Excel/CSV 처리
openpyxl==3.1.2
xlsxwriter==3.1.9
pandas==2.1.4

# 실시간 업데이트 (선택사항)
Flask-SocketIO==5.3.6

# 유틸리티
python-dateutil==2.8.2

# 테스트
pytest==8.2.2

# CJ 크롤링: Camoufox(위장 Firefox) headless — 서버 headless Chrome 봇 차단 대체
# 설치 후 1회 `scrapling install` 실행 필요 (Camoufox 브라우저 다운로드)
scrapling[fetchers]>=0.4,<0.5
# SSG 크롤링: TLS 지문(JA3) chrome 위장 — requests는 2026-07-31부터 403 차단
# (scrapling[fetchers]의 의존성이기도 하지만 SSG가 직접 import하므로 명시)
curl_cffi>=0.10
# SSG 쿠키 워밍업용 headful 브라우저를 디스플레이 없는 서버에서 띄우기 위함
# (별도로 시스템 패키지 필요: apt-get install -y xvfb)
pyvirtualdisplay>=3.0
